주요 단어

cluster: node machines들의 집합, 컨테이너에서 실행되는 worker node들과 master node들로 구성됨
nodes : 실제 혹은 가상 기기로 하나 혹은 그 이상의 pod를 실행하여 cluster와 상호작용 하는 것들
master node와 worker node가 있음
pods: 실제 앱이 실행되는 container와 실행에 필요한 리소스를 가짐
container : docker의 container
services: pod들의 논리 집합
